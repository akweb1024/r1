{
  "name": "tic-tac-toe-live",
  "version": "1.0.0",
  "scripts": {
    "start": "node server.js",
    "build": "rm -rf dist && mkdir dist && cp -r public/* dist/"
  },
  "dependencies": {
    "express": "^4.18.2",
    "socket.io": "^4.7.5"
  }
}